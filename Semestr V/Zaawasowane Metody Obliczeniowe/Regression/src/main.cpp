#include <cstdint>
#include <iostream>
#include <vector>

struct CarData {
    uint8_t miles_per_galoon;
    uint8_t cylinders;
    uint16_t displacement;
    uint16_t horsepower;
    uint16_t weight;
    float acceleration;
    uint8_t model_year;
    std::string car_name;
};


int main(int, char**){
    std::vector<CarData> = input();
    std::cout << "Hello, from Regression!\n";
}
